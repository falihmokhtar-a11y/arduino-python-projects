# Light Sensor (Arduino + Python)

This project reads light intensity from a phototransistor using Arduino
and sends the data to Python.

## Features
- Read analog light values from Arduino (A0)
- Send data via serial communication
- Detect light (hell) or dark (dunkel) using threshold
